<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>DevsAndStuff</title>
  <subtitle>For devs and geeks</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://devsandstuff.com/"/>
  <updated>2017-08-12T15:56:06.715Z</updated>
  <id>http://devsandstuff.com/</id>
  
  <author>
    <name>Efrain Tlapale Pulido</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>ES6 Goodies Intro: Destructuring Assignment</title>
    <link href="http://devsandstuff.com/ES6GoodiesIntro/"/>
    <id>http://devsandstuff.com/ES6GoodiesIntro/</id>
    <published>2017-01-21T06:09:14.000Z</published>
    <updated>2017-08-12T15:56:06.715Z</updated>
    
    <content type="html"><![CDATA[<img src="https://s19.postimg.org/bb7x25fs3/es6features.png"/><p>Hello, In this article series we’re going to take a look to some of my favorites ES6 features</p>
<p>To get started: <strong>What is ES6?</strong>  </p>
<p>ES6 or ECMAScript2015 or ES2015 is not the latest version of JavaScript, currently it is the ES7 standard, but ES6 is the one that brings a lot of exciting features to JavaScript</p>
<p>Before ES6 was released, the latest version of a JavaScript standard was ES5 in 2009, which introduced JSON, so, you can see that there is a gap between releases of 6 years, and as you can imagine it came whit a lot of new features and new ways of doing some things, fortunately, if you are working with ES5 and want to switch to ES6, you won’t feel like you’re programming in a completely new language, is more like feeling that you’re programming with JavaScript on steroids, and to prove that, we’re going to start with a simple but powerful feature: <strong>Destructuring Assignment</strong></p>
<hr>
<p>some variables declarations include the keywords <em>const</em> and <em>let</em>, these keywords are going to be explained in another post</p>
<hr>
<h2 id="Destructuring-Assignment"><a href="#Destructuring-Assignment" class="headerlink" title="Destructuring Assignment"></a>Destructuring Assignment</h2><p><em>Destructuring assignment</em> works for Arrays and Objects, but first let’s see what it is trying to accomplish  </p>
<p>In ES5 if you wanted to store some values of an array into variables, you have to do something like this:</p>
<figure class="highlight js"><figcaption><span>ES5</span></figcaption><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> someImportantValues = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>]</div><div class="line"><span class="keyword">var</span> firstValue = someImportantValues[<span class="number">0</span>]   </div><div class="line"><span class="keyword">var</span> secondValue = someImportantValues[<span class="number">1</span>]    </div><div class="line"><span class="built_in">console</span>.log(firstValue, secondValue)   <span class="comment">//Outputs: 1 2</span></div></pre></td></tr></table></figure>
<p>And in ES6 you could do something like this:</p>
<figure class="highlight js"><figcaption><span>ES6</span></figcaption><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">const</span> someImportanValues = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>];   </div><div class="line"><span class="keyword">const</span> [firstValue, secondValue] = someImportanValues</div><div class="line"><span class="built_in">console</span>.log(firstValue, secondValue)  <span class="comment">//Outputs: 1 2</span></div></pre></td></tr></table></figure>
<p>At first this might look weird, the first time I saw this notation I tough that it was making two copies of the object someImportanValues, but it just get the values from the array based on its positions</p>
<p>In objects Destructuring Assignment works based on matching keys:<br><figure class="highlight js"><figcaption><span>ES6</span></figcaption><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">const</span> object = &#123;<span class="attr">key</span>: <span class="number">1</span>, <span class="attr">value</span>: <span class="string">'some value'</span>, <span class="attr">secondValue</span>: <span class="string">'second value'</span>&#125;</div><div class="line"><span class="comment">//Storing properties of the object into variables</span></div><div class="line"><span class="keyword">const</span> &#123;key, value, secondValue, something&#125; = object</div><div class="line"><span class="built_in">console</span>.log(key, value, secondValue, something)</div><div class="line"><span class="comment">//Outputs 1 'some value' 'second value' undefined</span></div></pre></td></tr></table></figure></p>
<p>Note that the value of <strong>something</strong> is <em>undefined</em>, this is because no property name of the object match with “something”, so, no value is assigned to the const “something”</p>
<p>You can implement this feature in multiple ways</p>
<hr>
<h3 id="Managing-multiple-return-values"><a href="#Managing-multiple-return-values" class="headerlink" title="Managing multiple return values"></a>Managing multiple return values</h3><p>let’s say we have a function that returns an object that describes your birthday, but at some point you’re going to need to handle the day, month, and year separately</p>
<figure class="highlight js"><figcaption><span>ES5</span></figcaption><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> getBirthDay = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">  <span class="keyword">var</span> date = &#123;<span class="attr">day</span>: <span class="number">12</span>, <span class="attr">month</span>: <span class="number">12</span>, <span class="attr">year</span>: <span class="number">1996</span>&#125;</div><div class="line">  <span class="keyword">return</span> date</div><div class="line">&#125;</div><div class="line"><span class="comment">//Storing temporarily the day, month and year</span></div><div class="line"><span class="keyword">var</span> myBirthday = getBirthDay()</div><div class="line"><span class="keyword">var</span> day = myBirthday.day</div><div class="line"><span class="keyword">var</span> month = myBirthday.month</div><div class="line"><span class="keyword">var</span> year = myBirthday.</div><div class="line"><span class="built_in">console</span>.log(day, month, year) <span class="comment">//Outputs 12 12 1996</span></div></pre></td></tr></table></figure>
<p>with ES6 destructuring you can write it in a cleaner way</p>
<figure class="highlight js"><figcaption><span>ES6</span></figcaption><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">getBirthday</span> (<span class="params"></span>) </span>&#123;</div><div class="line">  <span class="keyword">const</span> date = &#123;<span class="attr">day</span>: <span class="number">12</span>, <span class="attr">month</span>: <span class="number">12</span>, <span class="attr">year</span>: <span class="number">1996</span>&#125;</div><div class="line">  <span class="keyword">return</span> date</div><div class="line">&#125;</div><div class="line"><span class="comment">//Storing temporarily the day, month and year</span></div><div class="line"><span class="keyword">const</span> myBirthday = getBirthday()</div><div class="line"><span class="keyword">const</span> &#123;day, month, year&#125; = myBirthday <span class="comment">//day = 12, month = 12, year = 1996</span></div></pre></td></tr></table></figure>
<p>The last two lines could be avoided by writing it this way:</p>
<figure class="highlight js"><figcaption><span>ES6</span></figcaption><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">const</span> &#123;day, month, year&#125; = getBirthday()</div><div class="line"><span class="built_in">console</span>.log(day, month, year) <span class="comment">//Outputs 12 12 1996</span></div></pre></td></tr></table></figure>
<hr>
<h3 id="Swaping-variables-values"><a href="#Swaping-variables-values" class="headerlink" title="Swaping variables values"></a>Swaping variables values</h3><p>It’s a simple thing, the following code just changes the values of x to the value of y, and the value of y to the value of x</p>
<figure class="highlight js"><figcaption><span>ES6</span></figcaption><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> x = <span class="number">4</span></div><div class="line"><span class="keyword">let</span> y = <span class="number">10</span></div><div class="line">[x,y] = [y,x]</div><div class="line"><span class="built_in">console</span>.log(x,y) <span class="comment">//Outputs 10 4</span></div></pre></td></tr></table></figure>
<hr>
<h3 id="Getting-values-from-parameters"><a href="#Getting-values-from-parameters" class="headerlink" title="Getting values from parameters"></a>Getting values from parameters</h3><p>This is my favorite implementation of <em>Destructing Assignment</em> because of the use that can be given to it in <em>React</em> and <em>Vue</em></p>
<p>Before diving into React, let’s look a little example:</p>
<p>We’re going to use a function that accepts an object as a parameter, but the function only need certain properties</p>
<figure class="highlight js"><figcaption><span>ES6</span></figcaption><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">formatPostHeader</span> (<span class="params">&#123;title, author&#125;</span>) </span>&#123;</div><div class="line">  <span class="keyword">return</span> $<span class="string">`&#123;title&#125; written by &#123;author&#125;`</span></div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="comment">//Post Object</span></div><div class="line"><span class="keyword">const</span> post : &#123;</div><div class="line">  title: <span class="string">'Blog post'</span></div><div class="line">  body: <span class="string">'Some dummy data'</span>,</div><div class="line">  author: <span class="string">'Efrain'</span>,</div><div class="line">  category: <span class="string">'Stuff'</span></div><div class="line">&#125;</div><div class="line"><span class="comment">//Call the function passing the post object</span></div><div class="line"><span class="keyword">const</span> header = formatPostHeader(post)</div><div class="line"><span class="built_in">console</span>.log(header) <span class="comment">//Outputs 'Blog post written by Efrain'</span></div></pre></td></tr></table></figure>
<p><em>The notation $`some text here {variable}` is going to be covered in another post</em></p>
<p>As we only need certain values from the object parametes, we use <em>destructuring assignment</em> to choose what we need</p>
<p>And this is the implementation in React <em>functional stateless components</em>. We try to keep this type of components as simple as possible, so, you can destruct the props object that it receives.</p>
<figure class="highlight js"><figcaption><span>ES6 without destructuring</span></figcaption><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">const</span> Post = <span class="function">(<span class="params">props</span>) =&gt;</span> &#123;</div><div class="line">  <span class="keyword">return</span> (</div><div class="line">    &lt;div&gt;</div><div class="line">      &lt;h1&gt; &#123;props.post.title&#125; &lt;<span class="regexp">/h1&gt;</span></div><div class="line"><span class="regexp">      &lt;p&gt; &#123;props.post.body&#125; &lt;/</span>p&gt;</div><div class="line">      &lt;h4&gt; Written by: &#123;props.post.author&#125; &lt;<span class="regexp">/h4&gt;</span></div><div class="line"><span class="regexp">    &lt;/</span>div&gt;</div><div class="line">  )</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>And we can improve it with <em>destructuring assignment</em></p>
<figure class="highlight js"><figcaption><span>ES6 with destructuring</span></figcaption><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">const</span> Post = <span class="function">(<span class="params">&#123;post&#125;</span>) =&gt;</span> &#123;</div><div class="line">  <span class="keyword">return</span> (</div><div class="line">    &lt;div&gt;</div><div class="line">      &lt;h1&gt; &#123;post.title&#125; &lt;<span class="regexp">/h1&gt;</span></div><div class="line"><span class="regexp">      &lt;p&gt; &#123;post.body&#125; &lt;/</span>p&gt;</div><div class="line">      &lt;h4&gt; Written by: &#123;post.author&#125; &lt;<span class="regexp">/h4&gt;</span></div><div class="line"><span class="regexp">    &lt;/</span>div&gt;</div><div class="line">  )</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h2 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h2><p><strong>Destructing Assignment</strong> is a simple but powerful feature, as you can see in the implementations examples, and is just the tip of the iceberg of all the goodies that ES6 brings to the JavaScript development  </p>
]]></content>
    
    <summary type="html">
    
      &lt;img src=&quot;https://s19.postimg.org/bb7x25fs3/es6features.png&quot;/&gt;&lt;p&gt;Hello, In this article series we’re going to take a look to some of my favo
    
    </summary>
    
      <category term="JavaScript" scheme="http://devsandstuff.com/categories/JavaScript/"/>
    
    
      <category term="ES6" scheme="http://devsandstuff.com/tags/ES6/"/>
    
  </entry>
  
</feed>
